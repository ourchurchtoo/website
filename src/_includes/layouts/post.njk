---
layout: /layouts/base.njk
---
<div class='row center-xs'>
  <div class='col-xs-12 col-md-7 text-left'>
    <div class='section section--standard section--post'>
      <div class='post__header text-center'>
        <h1>{{ title }}</h1>
        {% if ignore_info %}
        {% else %}
          <h4>written on {{ page.date | readableDate }} by {% for person in contributor %}<a class='link--color' href='{% for item in collections.contributors %}
            {% if item.data.title === person %}
              {{ item.data.permalink }}
            {% endif %}
          {% endfor %}'>{{ person }}</a>{% if loop.revindex0 === 0 %}{% elif loop.revindex0 === 1 %} &amp; {% else %}, {% endif %}
        {% endfor %}</h4>
        {% include 'partials/post-social.njk' %}
        {% endif %}
      </div>

      {% if image %}
        <img src='{{ image }}' />
        {% if artist %}
          <p class='text-center font--italic'>Art by <a class='link--color' href='{% for item in collections.contributors %}
          {% if item.data.title === artist %}
            {{ item.data.permalink }}
          {% endif %}
        {% endfor %}'>{{ artist }}</a></p>
        {% endif %}
      {% endif %}

      {{ content | safe }}

      {% if ignore_info %}
      {% else %}
        <div class='post__footer'>
          {% include 'partials/post-social.njk' %}
          <p>
            <a class='link--color' href='/archive/'><i class='fal fa-long-arrow-left'></i> View all posts</a>
          </p>
        </div>
      {% endif %}
    </div>
  </div>
</div>